# <프로젝트명> — prd.md

> 무엇을 · 누구를 위해 만드는가. **상류 SSOT** (이게 틀어지면 여기를 고친다).
> 규칙은 `> 규칙:`. capability/role 은 안정 ID/enum 을 갖는다 (하류 추적의 시작점).
> 이 파일은 `/gd-plan-prd` 인터뷰로 채워진다.

## 한 줄 요약
<이 서비스를 한 문장으로>

## 배경 / 맥락 (선택)
<왜 지금 이걸 만드나, 1-2줄>

## 타깃 페르소나 (1-3)
> 규칙: 1~3명. 너무 많으면 초점이 흐려진다.
- **<이름/역할>**: 니즈=<...>, 맥락=<언제/왜 쓰나>

## 핵심 가치 (Value Proposition)
<대안 대비 이 서비스가 주는 핵심 이득, 1~2문장>

## 차별점 (선택)
> 규칙: 대안 대비 핵심 차이 1줄. 불필요하면 생략.
- <...>

## 사용자 역할 + 접근 모델 (roles)
> 규칙: 역할을 enum 으로 명시 — capability/page 의 "주체"가 된다 (RBAC 수준의 디자인 입력).
> 규칙: access model 은 RBAC | ABAC | ReBAC 중 선언 + 디자인 함의 1줄. 정책 엔진 자체는 백엔드(범위 밖).
- **roles**: [User, Admin, ...]
- **access model**: RBAC | ABAC | ReBAC
- **디자인 함의**: <예: Admin 전용 화면 분리 / 소유자만 수정 버튼 노출 / "내 항목" 필터>

## Capabilities (사용자가 할 수 있는 것)
> 규칙: 각 항목 `[CAP-NN]` ID + **동사로 시작** + `(주체: 역할)`.
> 규칙: 모든 CAP 은 structure.md 의 page 로 `covers` 돼야 한다 (안 되면 review BLOCK).
- `[CAP-01]` <동사 ...> (주체: User)
- `[CAP-02]` <...> (주체: Admin)

## 범위 / 우선순위 (MVP)
> 규칙: 모든 capability 를 MVP(1차 출시 필수) / Later(2차 이후) 중 하나로 분류. CAP ID 로 참조 (라인 인라인 금지 — `(주체: 역할)` 추적 포맷 보존).
> 규칙: sitemap/page 가 "무엇을 먼저 그릴지" 의 입력. Out of Scope(=안 함)와 Later(=나중에 함)를 구분한다.
- **MVP**: [CAP-01], [CAP-02], ...
- **Later**: [CAP-..], ...

## 톤 키워드
> 규칙: design.md 톤과 연결. 통제된 어휘에서 선택 (예: 친근/전문/미니멀/대담/신뢰).
- <키워드 2-4개>

## 기기 / 플랫폼 (선택)
- <예: Web(반응형) / iOS / Android>

## 비기능 요구 (선택)
- 성능 <...> · 접근성 <...> · 다국어 <...>

## 성공 기준 (정량 + 목표값)
> 규칙: 각 기준은 측정 가능해야 한다 — 방향(↑/↓)만 두지 말고 **목표값**을 명시. 데이터가 없으면 **(가정치)** 또는 **(baseline 측정 후 확정)** 을 괄호로 표기. 가짜 정밀 금지 — 모르면 모른다고 명시.
1. <지표 = 목표값 (예: 웹 예약 비율 = 출시 3개월 내 30% (가정치))>
2. <...>

## 리스크 (선택)
- <주요 리스크> : <완화책>

## Out of Scope
- <이번에 안 하는 것>

<!-- 작성 예시는 @gen-design/plan 의 drafts/prd.template.md 참조 (동네 미용실 예약) -->
